[package]
name = "ruma-lean"
version = "0.1.0"
edition = "2021"
authors = ["Shane Jaroch <chown_tee@proton.me>"]
description = "Formally verified, dependency-free Matrix State Resolution v2 logic."

[dependencies]
# Zero dependencies! That's the point.
